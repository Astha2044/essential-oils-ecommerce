export const metadata = {
  title: "All Products | VS Naturals",
  description: "Explore our collection of pure essential oils and blends.",
};

export default function ProductsLayout({ children }) {
  return <>{children}</>;
}
