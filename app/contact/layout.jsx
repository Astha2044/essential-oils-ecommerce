export const metadata = {
  title: "Contact Us | VS Naturals",
  description: "Get in touch with the VS Naturals team for questions, support, and inquiries.",
};

export default function ContactLayout({ children }) {
  return <>{children}</>;
}
