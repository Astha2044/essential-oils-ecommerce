export const metadata = {
  title: "Our Journal & Blog | VS Naturals",
  description: "Read our latest articles on wellness, essential oils, and healthy living.",
};

export default function BlogLayout({ children }) {
  return <>{children}</>;
}
